{# templates/book/add.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}Add Book{% endblock %}

{% block body %}
    <h1>Add New Book</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('app_book') }}">Back to Book List</a>
{% endblock %}
